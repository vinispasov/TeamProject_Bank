package bank_product;

import tools.Entity;

public interface ClientYearsCheckable {
    void clientYearsCheck(int birthYear,int birtMonth,int birthDay);
}
//Check if the client is reach the age of majority
